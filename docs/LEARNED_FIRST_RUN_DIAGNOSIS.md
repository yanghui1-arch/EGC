# Learned Evidence v1 首轮标注诊断

## 2026-09-23真实重试后的更新

用户运行有界重试后，13次新请求处理7案，4案格式恢复keep、3案仍校验拒绝。总缓存为keep26/review9/failed41；其他38旧失败未执行。没有认证停止，不能将本次问题归为密钥无效。

旧停止器按“连续两批全部failed”判断，workers=1时等于连续两案拒绝，混淆了API故障与模型标注质量。现已拆分：401/403立即停派发；连续8案无可用响应的请求故障停止；连续20案重试后仍校验拒绝才触发独立质量停止。总HTTP预算和每案3次上限仍有效。

原响应核对：一条quote为原文唯一连续跨度，162字，只因160字工程限制拒绝；另有承接上文但短quote未重复姓名的事件。两条最终失败案例第2/3次choices内容完全相同。旧反馈仅给笼统错误码，新反馈列出全部字段路径、实际长度与来源匹配次数，不代写模型答案。日志增加failure_stage及本次分阶段统计，128项离线测试通过。

四案恢复keep不代表语义验证通过。已见公司行为与自然人目标归属、原文日期关系等需审阅的问题。按用户建议，下一版应支持有原文上下文依据的跨句主语恢复，见[设计](CONTEXT_BINDING_DESIGN.md)。新的schema尚未实现，先不继续v1付费重试，也不简单放宽字符串检查将旧失败冒充正确。

以下为重试前的原始诊断快照。

2026-09-23，用户询问`8/5936`为何同时显示`failed=42, keep=21, review=9`。助手只读检查正在更新的本机缓存，没有运行数据处理作业或API。

## 计数含义

原日志分母5936为这次准备发送的新请求数；累计状态包含启动前64条缓存。用户展示的第二行合计72=64+8，并不是刚发8次就失败42次。之前summary记录6000案例、已有64结果、上次新请求8次并因连续两批全失败停止；summary在进程结束才写，不能当作当前实时状态。

## 只读快照中的真实问题

取样时已有76份完成结果（不含同名pending标记）：keep22、review9、failed45。后续进程可能继续写入，本报告不是最终总量。

| 失败类别 | 数量 | 核对方法 |
|---|---:|---|
| 成功响应后未过本地校验 | 34 | 读取保存响应，JSON解码并只读重放同一validator；34份finish_reason均为stop |
| HTTP 401 | 8 | 缓存ApiRequestError安全诊断 |
| curl执行异常 | 3 | 缓存退出码3221225786；仅凭缓存不确定具体中断来源 |

34条校验失败的首次触发项：主体字符串不在该条quote中16；subject为空但relation却确定11；摘要类型/长度4；顶层schema2；原文引用唯一性/长度等1。**首次触发项不是全部错误数量；没有据此判断原文或语义一定错误。** HTTP401属于历史请求，随后有成功响应，不能推断当前所有请求都因密钥失败，更不应让用户在聊天里重发密钥。

当前主体局部引用约束与教师输出不匹配，直接跑完6000会隔离大量样本并可能造成选择偏差。要求用户先Ctrl+C暂停，保留缓存；不自动删除失败记录、重试收费或放宽校验宣称成功。keep也只是机器格式/引用通过，尚非语义准确率。

## 本轮修复与下一步

已改日志，明确输出cached_status、this_run_status和total_cached_status；summary也分开保存启动前/本次状态。以后校验失败保存固定白名单validation_code；不记录任意异常字符串或凭据。提示、API参数、原文引用标准、研究样本与旧缓存均未更改，这项日志修复**没有解决标注质量问题**。

上述为首次诊断时的决定。随后用户明确要求缺字段等错误应重试，并要求本机终端显示密钥核对。已新增validated-retry-v1，命令见[运行文档顶部](RUN_LEARNED.md)：每案最多3次总尝试，反馈输出错误后重新生成完整JSON；不是由代码填默认值。旧失败先备份，逐次保留请求/响应；keep/review跳过；401/403停止派发。总HTTP预算包含重试，当前仅对既有失败最多200请求。密钥仅由用户本机交互控制台直写确认，不进入应用日志；助手未读取用户密钥。126项离线测试通过，真实重试尚未运行，H4无新训练证据。
